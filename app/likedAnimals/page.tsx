export default function AccountFeaturesLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <section className=" min-h-[calc(100vh-172px)] xl:px-40 lg:px-14 py-4 px-3"></section>
    );
}
